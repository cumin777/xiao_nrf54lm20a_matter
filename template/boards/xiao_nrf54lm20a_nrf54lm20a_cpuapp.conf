# Temporary bring-up config for XIAO nRF54LM20A:
# keep the boot path observable and bypass factory-data-specific setup
# until the board adaptation is confirmed stable.

CONFIG_CHIP_FACTORY_DATA=n
CONFIG_CHIP_FACTORY_DATA_BUILD=n
